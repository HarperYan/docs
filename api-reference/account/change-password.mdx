---
title: "Change password"
sidebarTitle: "Change password"
description: "修改当前用户的登录密码"
api: "POST https://api.example.com/v1/account/password"
playground: "interactive"
---

修改当前用户的登录密码。修改成功后旧的会话令牌会被吊销。

## Headers

<ParamField header="Authorization" type="string" required>
  Bearer access token。
</ParamField>

## Body

<ParamField body="current_password" type="string" required>
  当前使用的密码。
</ParamField>

<ParamField body="new_password" type="string" required>
  新密码，至少 8 位，需包含字母和数字。
</ParamField>

## Response

<ResponseField name="success" type="boolean">
  是否修改成功。
</ResponseField>

<ResponseField name="message" type="string">
  提示信息。
</ResponseField>

<ResponseExample>

```json 200
{
  "success": true,
  "message": "Password updated successfully"
}
```

</ResponseExample>